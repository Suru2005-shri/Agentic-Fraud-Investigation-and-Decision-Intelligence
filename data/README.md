# Data directory

The committed `argus.db` is the compact benchmark cache used by the demo. Do not commit the raw challenge dataset unless the organizers explicitly require it.

For a full rebuild, place the provided `fraud.zip` here or set `ARGUS_DATA_DIR` to its extracted directory.
